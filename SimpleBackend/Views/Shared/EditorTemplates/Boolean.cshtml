@model bool?

@{
    var htmlAttributes = HtmlHelper.AnonymousObjectToHtmlAttributes(ViewData["htmlAttributes"]);

    var isChecked = Model.HasValue ? Model.Value : false;

    @Html.CheckBox("", isChecked, htmlAttributes)
}

